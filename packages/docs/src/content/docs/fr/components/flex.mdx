---
title: Flex
description: Composant de disposition flexible pour créer des mises en page réactives.
tableOfContents: false
---

import {
  BasicDemo,
  DirectionDemo,
  JustifyDemo,
  AlignDemo,
  GapDemo,
  WrapDemo,
  CenteringDemo,
  NestedDemo,
} from '../../../../components/FlexDemo'
import FlexDemoSource from '../../../../components/FlexDemo.tsx?raw'
import Example from '../../../../components/Example.astro'

Composant de disposition flexible pour créer des mises en page réactives.

## Importation

```tsx
import { Flex } from 'asterui'
```

## Exemples

<div class="examples">

<Example title="De base" description="Disposition flex horizontale par défaut." source={FlexDemoSource} fn="BasicDemo">
  <BasicDemo client:load slot="demo" />
</Example>

<Example title="Direction" description="Contrôler la direction flex avec la prop direction." source={FlexDemoSource} fn="DirectionDemo">
  <DirectionDemo client:load slot="demo" />
</Example>

<Example title="Justification du contenu" description="Distribuer les éléments le long de l'axe principal." source={FlexDemoSource} fn="JustifyDemo">
  <JustifyDemo client:load slot="demo" />
</Example>

<Example title="Alignement des éléments" description="Aligner les éléments le long de l'axe transversal." source={FlexDemoSource} fn="AlignDemo">
  <AlignDemo client:load slot="demo" />
</Example>

<Example title="Tailles d'espacement" description="Contrôler l'espacement entre les éléments." source={FlexDemoSource} fn="GapDemo">
  <GapDemo client:load slot="demo" />
</Example>

<Example title="Retour à la ligne" description="Permettre aux éléments de passer à la ligne suivante." source={FlexDemoSource} fn="WrapDemo">
  <WrapDemo client:load slot="demo" />
</Example>

<Example title="Centrage du contenu" description="Centrer le contenu horizontalement et verticalement." source={FlexDemoSource} fn="CenteringDemo">
  <CenteringDemo client:load slot="demo" />
</Example>

<Example title="Flex imbriqué" description="Combiner des conteneurs flex pour des mises en page complexes." source={FlexDemoSource} fn="NestedDemo">
  <NestedDemo client:load slot="demo" />
</Example>

</div>

## API

### `Flex`

| Propriété | Description | Type | Défaut |
|----------|-------------|------|---------|
| `direction` | Direction flex | `'row' \| 'column' \| 'row-reverse' \| 'column-reverse'` | `'row'` |
| `justify` | Justifier le contenu le long de l'axe principal | `'start' \| 'end' \| 'center' \| 'between' \| 'around' \| 'evenly'` | - |
| `align` | Aligner les éléments le long de l'axe transversal | `'start' \| 'end' \| 'center' \| 'baseline' \| 'stretch'` | - |
| `wrap` | S'il faut envelopper les éléments | `boolean \| 'wrap' \| 'nowrap' \| 'wrap-reverse'` | `false` |
| `gap` | Espacement entre les éléments | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| number` | - |
| `flex` | Raccourci flex pour le conteneur | `'1' \| 'auto' \| 'initial' \| 'none'` | - |
| `inline` | Utiliser inline-flex au lieu de flex | `boolean` | `false` |
| `minHeight` | Hauteur minimale | `'screen' \| 'full' \| 'fit' \| 'min' \| 'max'` | - |
| `minWidth` | Largeur minimale | `'full' \| 'fit' \| 'min' \| 'max'` | - |
| `className` | Classes CSS supplémentaires | `string` | - |
| `data-testid` | ID de test pour les tests | `string` | - |

## Notes d'utilisation

- Flex est un utilitaire de mise en page sans exigence d'accessibilité spécifique
- Maintient la structure sémantique des éléments enfants
- L'espacement correspond à l'espacement Tailwind : xs=1, sm=2, md=4, lg=6, xl=8
